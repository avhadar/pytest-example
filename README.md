# pytest-example
A complex number class to show the use of pytest

This class uses some of the more advanced Python features and is tested using pytest.

## Usage
Clone this repository.

Then run 'python -m pytest'

## Pytest features used
* parametrized tests (using @pytest.mark.parametrize)
* fixtures (using @pytest.fixture)
* checking for errors (using pytest.raises)
